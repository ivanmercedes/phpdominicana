<section class="py-20 bg-gradient-to-b from-white to-primary/20">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="text-center mb-16">
      <h2 class="text-4xl md:text-5xl font-bold text-gray-900 mb-6">
        Nuestra Comunidad Trabaja
      </h2>
      <p class="text-lg text-gray-600 max-w-3xl mx-auto leading-relaxed">
        Conozca los proyectos desarrollados por nuestros miembros:
      </p>
    </div>

    <!-- Filter Tabs -->
    <div class="flex justify-center mb-12">
      <div
        class="bg-white/80 backdrop-blur-sm rounded-full p-1 shadow-lg border border-white/50"
      >
        <div class="flex space-x-1">
          <button
            class="px-6 py-3 rounded-full text-sm font-semibold bg-primary text-white shadow-md"
          >
            Todos
          </button>
          <button
            class="px-6 py-3 rounded-full text-sm font-semibold text-gray-600 hover:text-gray-900 transition-colors"
          >
            Publicados
          </button>
          <button
            class="px-6 py-3 rounded-full text-sm font-semibold text-gray-600 hover:text-gray-900 transition-colors"
          >
            En Desarrollo
          </button>
        </div>
      </div>
    </div>

    <!-- Projects Grid -->
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-8 max-w-6xl mx-auto">
      <!-- Reverse Job Board -->
      <div
        class="bg-white/90 backdrop-blur-sm rounded-3xl p-8 shadow-xl hover:shadow-2xl transition-all duration-300 transform hover:-translate-y-2 border border-white/50"
      >
        <h3 class="text-2xl font-bold text-gray-900 mb-3">Reverse Job Board</h3>
        <p class="text-sm text-gray-500 mb-6 font-medium">Fermin Perdomo</p>
        <p class="text-gray-600 leading-relaxed mb-8">
          Esta es una bolsa de trabajo inversa para desarrolladores de PHP. En
          lugar de que las empresas publiquen trabajos, los desarrolladores
          describen su trabajo ideal y las empresas se comunican con ellos.
        </p>
        <button
          class="text-primary font-semibold hover:text-secondary transition-colors border-b border-blue-600 hover:border-blue-700 pb-1"
        >
          Visitar el Proyecto
        </button>
      </div>

      <!-- Dev Directory -->
      <div
        class="bg-gradient-to-br from-secondary to-primary rounded-3xl p-8 shadow-xl hover:shadow-2xl transition-all duration-300 transform hover:-translate-y-2 text-white"
      >
        <h3 class="text-2xl font-bold mb-3">Dev Directory</h3>
        <p class="text-blue-100 mb-6 font-medium">Ricardo Vargas</p>
        <p class="text-blue-50 leading-relaxed mb-8">
          Directorio oficial de programadores de la Comunidad de PHP Dominicana.
        </p>
        <button
          class="bg-white text-primary px-6 py-3 rounded-full font-semibold hover:bg-blue-50 transition-all duration-300 shadow-lg hover:shadow-xl transform hover:-translate-y-1"
        >
          Visitar el Proyecto
        </button>
      </div>
    </div>
  </div>
</section>
